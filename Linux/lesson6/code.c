#include <stdio.h>

#define MAX 100

int main()
{
    printf("hello bit, hello : %d\n", MAX);
    printf("hello bit, hello : %d\n", MAX);
    printf("hello bit, hello : %d\n", MAX);
    printf("hello bit, hello : %d\n", MAX);
    printf("hello bit, hello : %d\n", MAX);
    printf("hello bit, hello : %d\n", MAX);
    printf("hello bit, hello : %d\n", MAX);
    printf("hello bit, hello : %d\n", MAX);
    return 0;
}